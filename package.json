{
  "version": "1.0.0",
  "scripts": {
    "build": "ncc build",
    "prettier:check": "prettier --check .",
    "prettier:write": "prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "gray-matter": "^4.0.3"
  }
}
